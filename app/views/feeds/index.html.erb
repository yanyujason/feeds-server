<h1>Listing articles</h1>

<table>
    <tr>
        <th>Title</th>
        <th>Text</th>
    </tr>

    <% @entries.each do |entry| %>
        <tr>
            <td><%= entry.title %></td>
            <td><%= entry.content %></td>
        </tr>
    <% end %>
</table>